import { AccountSettingsPage } from '@/components/account/account-pages';

export const metadata = {
  title: 'Settings',
};

export default function SettingsPage() {
  return <AccountSettingsPage />;
}
